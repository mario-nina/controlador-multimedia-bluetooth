#pragma once

/**
 * @brief Módulo de control de indicadores LED.
 *
 * Gestiona el LED azul (estado Bluetooth) y el LED rojo (estado batería)
 * mediante patrones de encendido, apagado y parpadeo.
 */

/**
 * @brief Inicializa el módulo de control de LEDs.
 */
void control_leds_init(void);
